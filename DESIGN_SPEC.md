# 来财记账 · 三版 App 原型设计规范（DESIGN_SPEC）

> 本文件是三版原型（简约版 / 时尚版 / 奢华版）的**唯一共同输入**。三个构建子代理各自独立工作、只看本文件、互不参考。所有屏幕内容、数据、交互与设备框规格以本文件为准。

---

## 1. 产品与内容事实（必须一致）

- **产品名**：来财记账
- **一句话定位**：Android 实时自动记账 + 跨端账单导入 + 云端 LLM 周期分析的「确定优先」记账应用。
- **架构铁律（原型中要有对应视觉表达）**：确定性代码管金额账本；LLM 只做解释与建议；云端分析需显式授权、可关闭；结论可下钻到原始交易。
- **平台**：Android 先行（自动记账），iOS 后补（账本+导入）。
- **对外承诺句**（可放入副标题/详情页）：自动归集可获取的交易，并提供便捷导入与确认。

### 1.1 统一演示数据（不得更换，只改视觉呈现方式）

| 资产 | 数值 |
|---|---|
| 招商银行 ♥8899 | ¥26,480.52 |
| 微信零钱 | ¥1,206.35 |
| 支付宝 | ¥843.20 |
| 现金 | ¥520.00 |
| 本月支出 | ¥8,432.60 |
| 本月收入 | ¥25,000.00 |
| 本月结余 | +¥16,567.40 |

**最近流水（首页 & 流水页使用，按时间倒序）**：
- 8月17日 09:12 · 瑞幸咖啡 -¥32.50 · 微信零钱 · 餐饮 · ✅ 自动
- 8月17日 08:40 · 地铁 -¥6.00 · 支付宝 · 交通 · ✅ 自动
- 8月16日 20:23 · 盒马鲜生 -¥128.60 · 支付宝 · 购物 · ✅ 自动
- 8月16日 12:05 · 美团外卖 -¥45.80 · 微信零钱 · 待确认
- 8月15日 18:30 · 星巴克 -¥38.00 · 招商银行 · 餐饮 · ✅ 自动
- 8月14日 10:00 · 工资入账 +¥25,000.00 · 招商银行 · 收入 · ✅ 自动
- 8月12日 22:10 · 滴滴出行 -¥18.40 · 微信零钱 · 交通 · ✅ 自动
- 8月10日 15:00 · 京东 -¥299.00 · 招商银行 · 待确认

**待确认区（3 条）**：
1. 美团外卖 -¥45.80 · 8月16日 · 原因：分类不明确
2. 京东 -¥299.00 · 8月10日 · 原因：账户未知（未匹配到既存账户）
3. 现金 -¥120.00 · 8月13日 · 原因：低置信度，疑似重复

**月度分类占比（分析页使用）**：餐饮 42% / 购物 18% / 交通 14% / 居住 12% / 娱乐 9% / 其他 5%

**分析页确定性指标**：支出 ¥8,432.60（环比 -12.4%）· 收入 ¥25,000.00 · 结余 +¥16,567.40 · 餐饮超预算 +8% · Top 商户：星巴克 6 笔 ¥186.00

**LLM 报告摘要（分析页）**：
> 「本月支出 8,432.60 元，环比下降 12.4%——主要来自餐饮收敛。餐饮支出超预算 8%，集中在星巴克（6 笔 186 元）。建议：下周减少咖啡类外带，预计每月可节省约 120 元。」
> 附注：覆盖度 已确认 96%（2 笔待确认未计入）｜ 云端分析已授权 · 可关闭

---

## 2. 屏幕地图（每版都必须包含以下 6 屏能力）

每版是一个**可交互的单文件原型**：底部 5-Tab 导航 + 首页演示屏，外加「自动记账动效演示」。具体：

| 屏 | 内容 | 必带交互 |
|---|---|---|
| **S0 自动记账演示**（首页顶部卡片/独立区） | 演示一次完整链路：微信支付通知动画 → 通知正文浮现 → 「已解析：餐饮 -27.50 瑞幸咖啡」 → 入账成功打勾；或演示「低置信 → 进入待确认」的分支 | 点击「播放演示」按钮重播动画；动画含 3-4 个关键帧的时序 |
| **S1 首页·总览** | 本月结余/支出/收入三数字；待确认角标（有 3 条）；分类占比迷你条；最近流水 3-5 条 | Tab 切换；点流水行 → 详情弹层；点待确认 → 跳待确认屏（或弹确认面板）；数字加载动画 |
| **S2 流水·列表** | 按日分组的流水全表（用 1.1 数据）；搜索/筛选区（可展开）；顶部「导入账单」入口 | 点行 → 交易详情弹层（含来源「微信通知自动捕获/手动/导入」，可编辑金额、可删除）；筛选 chips 可点切换 |
| **S3 记一笔·手动记账** | 金额键盘（0-9、.、删除、确认）；收支切换（支出/收入/转账）；分类宫格（餐饮/交通/购物/居住/娱乐…）；账户选择 | 按键实时更新金额显示；选分类高亮；「保存」按钮 → 成功 toast → 流水新增 |
| **S4 分析** | 确定性指标面板（支出/收入/结余/环比/分类占比）；LLM 周��报告卡片（见 1.1 摘要，含覆盖度声明）；自然语言问答输入框（预置问句 chips：「这个月外卖花了多少？」） | 「生成报告」→ 带加载态（转圈+打字机效果）→ 展示报告；问答 chips 点击 → 模拟回答（预置结果）；「数据范围」说明可展开 |
| **S5 我的·设置** | 账户列表（4 账户含余额）；通知监听状态开关（Android 状态：已开启）；云端分析授权开关（默认开，带「显式授权」说明）；备份/恢复入口；隐私声明短句 | 开关可拨动；点账户 → 账户详情弹层；授权开关关闭 → 出现提示「将仅保留本地确定性指标」 |

**导航结构**：底部 Tab 5 个 = 首页 / 流水 / 记一笔（中央突出大按钮）/ 分析 / 我的。Tab 点击切换对应屏（全部在同一个 iPhone 框内完成，不另外开页）。S0 演示屏放置在首页顶部卡片区（可收起的「自动记账演示」卡）。

---

## 3. iPhone 设备框规格（必须使用下方模板，禁止自行发明）

逻辑尺寸 390×844。离屏时浏览器窗口可任意缩放，设备框保持固定像素尺寸。

```html
<div class="iphone">
  <!-- Dynamic Island：固定 124×36，绝对定位于顶部居中 -->
  <div class="island"></div>
  <!-- 状态栏：高 54px，时间居左(避开岛)，信号/电量居右(避开岛) -->
  <div class="statusbar">
    <span class="sb-time">9:41</span>
    <div class="sb-icons"><span class="sb-i">▂▄▆</span><svg class="sb-batt">…</svg></div>
  </div>
  <!-- 屏幕内容区 -->
  <div class="screen"><!-- 全部 App UI 在此 --></div>
  <!-- Home Indicator：134×5 底部居中 -->
  <div class="homeind"></div>
</div>
```

```css
.iphone {
  position: relative; width: 390px; height: 844px; flex: none;
  border-radius: 52px; background: #000;
  box-shadow: 0 30px 80px rgba(0,0,0,.45), 0 0 0 2px #222, inset 0 0 0 2px #000;
  overflow: hidden;
}
.island { position: absolute; top: 12px; left: 50%; transform: translateX(-50%);
  width: 124px; height: 36px; background: #000; border-radius: 20px; z-index: 60; }
.statusbar { position: absolute; top: 0; left: 0; right: 0; height: 54px; z-index: 50;
  display: flex; justify-content: space-between; align-items: flex-end;
  padding: 0 30px 8px; font-size: 15px; font-weight: 600; color: #fff; }
.sb-icons { display: flex; align-items: center; gap: 6px; font-size: 11px; letter-spacing: -1px; }
.homeind { position: absolute; bottom: 8px; left: 50%; transform: translateX(-50%);
  width: 134px; height: 5px; border-radius: 3px; background: #fff; opacity: .9; z-index: 60; }
.screen { position: absolute; inset: 0; z-index: 10;
  border-radius: 48px; overflow: hidden; background: inherit; }
```

**注意**：
- Dynamic Island 是**纯装饰黑条**，永远置顶，App 内容不得覆盖它；岛两侧 133px 内放状态栏左右元素。
- 状态栏文字颜色可随屏背景明暗调整，但岛永远黑色。
- 屏幕内容滚动必须在 `.screen` 内部（`overflow-y: auto`），不可滚动整个页面。
- 状态栏内容可精简：左「9:41」，右「信号 ▂▄▆＋电量 ▮▮▮▯」即可，不必每次手绘 SVG。

---

## 4. 交互与动画要求

**交互（每版都必须实现）**：
1. 底部 5-Tab 切换，切换有轻微过渡（滑动/淡入皆可，须可见）
2. 点流水行 → 弹出交易详情（modal/底部抽屉均可），含：金额、分类、账户、时间、来源标签（「微信通知自动捕获 / 手动 / 导入」）、备注、「编辑」与「删除」按钮（点击有可感知反馈）
3. S0 演示播放按钮可重播；S4「生成报告」有 ≥1.2s 加载态（转圈 + 打字机）
4. S3 金额键盘按键实时更新显示；保存 → toast「已记一笔」
5. S5 开关可拨动且状态联动文案

**动画（每版都必须有，克制而明确）**：
- **页面加载动画**：首屏元素 2-3 个关键帧（如数字滚动计数、卡片淡入上移、结余数字从 0 计数到 16,567.40）
- **自动记账演示动画**：S0 播放链路的 3-4 帧时序（通知滑入 → 高亮解析字段 → 入账打勾 / 或跳转待确认），必须用 CSS animation + 少量 JS 时序控制
- **数值动画**：S1/S4 关键数字用 count-up（requestAnimationFrame）或 CSS 过渡
- 禁止散落的微交互堆砌：动画只服务于「自动记账链路」「数据呈现」两处叙事

---

## 5. 三版风格方向（每个子代理只做自己那一版）

> 三版内容 100% 相同，**布局骨架与视觉语言必须结构性互异**（不是换色换字体的换皮）。每版交付时在 HTML 顶部注释写一句「form 来自内容的哪里」（母题来源）。

### 5.1 简约版（Minimal）→ 输出 `prototypes/minimal.html`

- **母题**：「墨与账本」——宣纸白底 + 墨色文字 + 一点朱砂 accent；界面像一册被认真誊写的账本。
- **色板**：底 `#F7F4EE`（宣纸）/ 主文字 `#1E1C19`（墨）/ accent `#B4552D`（朱砂）/ 次级文字 `#8A857B` / 分隔线 `#E5E0D4` / 语义绿 `#3E6B4F`（收入）/ 语义红 `#B4552D`（支出，同朱砂）
- **字体**：display 用宋体系衬线 `"Songti SC","Noto Serif SC",serif`；正文用 `-apple-system,"PingFang SC",sans-serif`。数字用衬线大号（体现账本感）
- **布局骨架**：顶部大标题区（月份 + 结余大数字居中），下面一条细分隔线，卡片**无边框无阴影**（仅细线分隔的列表式），Tab 栏为极简文字型（选中项墨色加粗 + 朱砂小点）
- **视觉气质**：安静、留白、克制的排版密度；圆角极小（4-8px）；图标用细线型（stroke 1.5）
- **数据呈现**：分类占比用细线堆叠条（无内阴影）；报告卡是一页「纸面」效果
- **form 说明**：数字陈列像账本墨迹，分隔线像账本表格线——来自「记账」这件事本身。

### 5.2 时尚版（Fashion）→ 输出 `prototypes/fashion.html`

- **母题**：「流动的金币」——高对比潮牌，青柠黑撞色 + 流动的圆润卡片；界面像年轻金融科技（参考 Revolut / 新消费品牌视觉，但**避免通用紫渐变**）。
- **色板**：底 `#0E0F0D`（石墨黑）/ 主文字 `#F5F5F0` / accent 青柠 `#C8F135` / 次级文字 `#9AA0A0` / 语义绿 `#C8F135`（收入）/ 语义红 `#FF6B4A`（支出）/ 卡片 `#1A1C19` / 描边 `#2A2D29`
- **字体**：display 用无衬线粗体 `-apple-system,"PingFang SC",sans-serif` 900 weight、字距收紧；数字用等宽感 `"SF Mono",ui-monospace` 做金额（科技感）
- **布局骨架**：首屏为整屏「本月结余」大卡（青柠大数字）+ 右侧收支双竖卡；流水行是圆角胶囊式；Tab 栏为漂浮药丸型（选中项青柠底黑字）；加载/播放按钮用高饱和动效
- **视觉气质**：年轻、利落、有冲击力；圆角 16-20px；粗体标签 chips；装饰克制（不做霓虹 glow 堆砌，用青柠单 accent + 高对比）
- **数据呈现**：占比用大圆角进度条（青柠/灰）；报告卡为暗色面板 + 青柠高亮要点
- **形态补充**：手机框内可用一条「Live」小标（示意实时自动记账），但不得加底部进度条伪 chrome
- **form 说明**：青柠=「来财」的生机与流通感，撞色黑底=交易发生的瞬时冲击——来自「实时自动」的即时性。

### 5.3 奢华版（Luxury）→ 输出 `prototypes/luxury.html`

- **母题**：「鎏金钱庄」——深墨绿底 + 金线 + 衬线排版，界面像一间体面的老钱庄账房（参考珠宝/私行视觉）。
- **色板**：底 `#101A17`（深墨绿）/ 主文字 `#EDE7D8`（暖米白）/ 金 `#C7A25B`（主 accent）/ 次级文字 `#9AA79E` / 收入金绿 `#8FB573` / 支出铜红 `#C96A4A` / 卡片 `#16211D` / 金线 `rgba(199,162,91,.45)`
- **字体**：display 用衬线 `"Songti SC","Noto Serif SC",serif` 或英文衬线 `"Playfair Display",serif` + 中文宋体 fallback，**字距加宽**；正文 `-apple-system,"PingFang SC",sans-serif`
- **布局骨架**：顶部金线鎏金条 + 居中「结余」衬线大字（字距宽、颜色米白）；资产卡带 1px 金描边与四角金属角饰；列表行间金细线；Tab 栏为衬线文字型 + 金色小菱形 ◆ 指示；报告卡像一封金边信笺
- **视觉气质**：稳重、尊贵、低密度；圆角 2-6px（近乎直角）；分隔与装饰全部用金线而非阴影
- **数据呈现**：占比用金色细环（如圆环图）；金额数字衬线、千分位、字距加宽
- **form 说明**：鎏金与墨绿=「财」的传统尊贵意象，细金线=帐目的规矩与体面——来自「财富管理」的郑重感。

---

## 6. 硬性约束（三版都必须满足）

1. **单文件**：`.html` 自包含，全部 CSS/JS inline，双击 `file://` 直接可开；**不引用外网资源**（无 CDN 字体/图片/脚本）；图片一律用 CSS/SVG 内联（本原型无真实照片需求，用纯几何与文字排版达成——记账工具类内容不依赖照片）
2. **可读性底线**：正文 ≥14px、标签/注释 ≥12px、正文对比度 ≥4.5:1；留白必须是构图不是内容缺席
3. **iPhone 框**：必须用第 3 节的模板与尺寸；内容滚动在 `.screen` 内
4. **信息密度**：产品卖点是 AI/数据/自动记账 → 走**高密度型**：每屏 ≥3 处**有内容的**差异化信息（如来源标签、置信度、覆盖度、环比、待确认角标），禁止装饰性 icon 堆砌
5. **反 AI slop**：不用激进紫渐变、不用 emoji 当图标（金额/图标用文字或细线 SVG）、不用「圆角卡+左彩色 border」烂大街组合、不用深蓝 `#0D1117`+霓虹 glow；图标不足时用「文字标签 + 细线框」诚实占位
6. **文案规范**：中文引号用「」；金额带千分位与 ¥；来源标签格式统一（「微信自动捕获」「手动」「导入」）
7. **一个 120% 细节签名**：每版至少留一处「值得截图」的精致细节（如简约版结余数字的衬线对齐、时尚版的青柠 Live 胶囊、奢华版的金色环形资产环）
8. **HTML 结构**：顶部 `<head>` 中放 `<style>`，底部放 `<script>`；文件开头 HTML 注释写明：版本风格、母题来源（form 来自内容的哪里）、假设清单
9. **性能**：单文件 < 900 行（CSS 可压缩书写）；JS 无第三方依赖；无 `scrollIntoView`

---

## 7. 自检清单（构建完成后逐项自查，全部通过才交付）

- [ ] 文件为单文件且 `file://` 可打开
- [ ] iPhone 框符合第 3 节规格（岛 124×36、状态栏 54、homeind 134×5），岛未被内容覆盖
- [ ] 6 屏能力齐全（S0 演示 / S1 首页 / S2 流水 / S3 记一笔 / S4 分析 / S5 我的），Tab 切换正常
- [ ] 数据与第 1.1 节完全一致（金额、商户、日期、待确认 3 条、分类占比、报告摘要）
- [ ] 交互齐备：流水详情弹层、S0 重播、S4 生成报告加载态、S3 键盘+toast、S5 开关联动
- [ ] 动画齐备：加载计数动画 + 自动记账链路帧动画 + count-up
- [ ] 可读性：正文 ≥14px、对比度 ≥4.5:1、滚动在 `.screen` 内
- [ ] 无外网资源、无 emoji 图标、无紫渐变；有一个 120% 细节签名
- [ ] 布局骨架与另外两版结构性不同（只需保证自己不是通用模板骨架）