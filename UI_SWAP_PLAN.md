# 三版 UI 一致性评估与「可更换 UI」落地架构

> 版本 v1.0 ｜ 2026-08-17 ｜ 对应问题：① 三版功能点是否相同？② 后期能否直接更换 UI？

---

## 一、结论速览

| 问题 | 结论 |
|---|---|
| ① 三版功能点是否相同 | **是，功能点 100% 一致**（同一 PRD 派生、同一份数据、同一套屏幕地图与交互），差异仅在设计语言（视觉骨架）。三版为「同芯异壳」。 |
| ② 后期能否直接更换 UI | **可以，但不能靠「删掉旧文件换新文件」实现**——因为三版是结构性不同的骨架（非换色换字体的皮）。落地方式是 **Design Token 主题化**：一套引擎 + 三套主题文件，运行时一键切换。 |

---

## 二、问题①：功能点一致性核查（实测）

对三版原型做静态与运行核查，结果如下：

### 2.1 统一功能清单（PRD 派生，三版全覆盖）

| 功能点（PRD 编号） | minimal | fashion | luxury |
|---|---|---|---|
| S0 自动记账演示动画（通知→解析→入账） | ✅ | ✅ | ✅ |
| S1 首页：结余/支出/收入 + 待确认角标 + 分类占比 | ✅ | ✅ | ✅ |
| S2 流水列表：分组 + 筛选 + 搜索 | ✅ | ✅ | ✅ |
| 流水详情弹层（商户/分类/账户/来源/置信度/编辑/删除） | ✅ | ✅ | ✅ |
| S3 记一笔：金额键盘 + 收支/转账切换 + 分类宫格 + 账户 | ✅ | ✅ | ✅ |
| S4 分析：确定性指标 + LLM 报告加载态 + 问答 chips | ✅ | ✅ | ✅ |
| S5 我的：账户列表 + 通知监听开关 + 云端授权开关 + 备份 | ✅ | ✅ | ✅ |
| 5-Tab 导航 + 页面过渡 | ✅ | ✅ | ✅ |
| count-up / 加载动画 | ✅ | ✅ | ✅ |

### 2.2 统一数据（三版数值一致）

瑞幸咖啡 -32.50 ✓ ｜ 结余 +16,567.40 ✓ ｜ 支出 8,432.60 ✓ ｜ 待确认 3 条 ✓ ｜ 微信通知自动捕获来源标签 ✓
（luxury 无「doSearch」搜索输入框按钮、但保留筛选；fashion 搜索用独立函数——微差见 §2.3）

### 2.3 已知差异（不影响功能一致性，需在合并时统一）

| 差异 | minimal / luxury | fashion |
|---|---|---|
| Tab 切换函数 | `goTab()` | `go()`（事件委托而非 onclick） |
| 数据渲染 | `renderLedger()/txRow()` | `applyFilter()/fmt()` |
| 搜索 | 有搜索输入框 `doSearch()` | 有搜索输入框但走 `applyFilter()` |
| 交互绑定 | 混合 onclick + addEventListener | 全部 addEventListener（更现代） |
| 收入支出 | 独立 `renderHome()` | 集成在渲染管线 |

**结论**：三版是「同一套数据模型与服务逻辑，三种不同的表现层实现」。功能一致 ✅，但**代码没有共用**——这正是问题②的关键。

---

## 三、问题②：可更换 UI 的三个层次（关键洞察）

「更换 UI」有三种理解，技术成本天差地别：

### 层次 A：换颜色/字体/间距（纯换肤）
- 三版差异的 **~60%** 是颜色、字体、圆角、间距、描边。
- **成本极低**：CSS Custom Properties（CSS 变量）即可实现运行时切换。
- ✔️ 但**不能覆盖全部差异**——三版有结构性区别（见层次 B）。

### 层次 B：换组件/布局骨架（半换肤）
- 三版差异的 **~30%** 是骨架：首页布局（居中大字 vs 整屏大卡）、Tab 形态（文字型 vs 药丸型 vs 菱形）、流水行（细线 vs 胶囊 vs 金线）。
- **需要组件级抽象**：同一数据渲染成不同布局组件。
- 这无法靠 CSS 变量完成，需要 **Slot / 模板替换** 或 **组件条件渲染**。

### 层次 C：换引擎（完全重建）
- 若连交互逻辑、数据流都不同 —— 那等于重写 App。
- 三版**不需要**这一层：交互逻辑与数据是共用的。

**结论**：三版满足「同芯异壳」，因此采用 **A（Token 层）+ B（组件层）** 组合即可实现「后期直接更换 UI」：

```
┌─────────────────────────────────────┐
│  App 引擎（共用一次）                    │
│  · 数据模型（交易/账户/待确认）            │
│  · 状态机（Tab、Modal、Demo 播放）       │
│  · 交互逻辑（确认/删除/报告/开关）         │
├─────────────────────────────────────┤
│  组件层（三版各一套）  ← 可替换          │
│  · HomeLayout / TxRow / TabBar /      │
│    KeyPad / ReportCard              │
├─────────────────────────────────────┤
│  Design Token（三版各一套） ← 可替换     │
│  · colors / fonts / radius / spacing /│
│    borders / shadows / motion        │
└─────────────────────────────────────┘
        ▲ 切换 UI = 换「Token」+「组件包」
```

---

## 四、落地架构方案（推荐）

### 方案一：Token 主题化（推荐，成本最低、收益最大）

把三版收敛为**一个 React/Vue 单文件工程**（或原生自定义元素），视觉差异全部抽成主题文件：

```
src/
├── app.js            # 引擎：数据 + 状态 + 交互（唯一一份）
├── components/       # 通用组件（按钮/列表/弹层/键盘）
├── themes/
│   ├── minimal.css   # 简约：宣纸+朱砂 + 文字型 Tab + 细线列表
│   ├── fashion.css   # 时尚：石墨+青柠 + 药丸 Tab + 胶囊行
│   └── luxury.css    # 奢华：墨绿+鎏金 + 菱形 Tab + 金线行
├── layouts/
│   ├── minimal.home.jsx
│   ├── fashion.home.jsx
│   └── luxury.home.jsx
└── settings.js       # 当前主题 = localStorage（默认 minimal）
```

- **运行时切换**：设置页放「主题」选择器 → 加载对应 `.css` + `layouts/*` → 一键换肤。
- **60% 差异免费**（Token），**30% 差异低成本**（布局组件），0% 重写引擎。
- 开发后期若要发布其中一版，直接把该主题设为默认即可，其余主题保留为「换肤彩蛋」。

### 方案二：保留三版独立文件 + 构建期选型（当前阶段推荐）

不够优雅但零重构风险：
- 三版保持独立（现在就是），`index.html` 三入口并存。
- 到真正开发时，用**其中一版**作为基线（建议：先评审选定风格→以该版 HTML 为 base→抽出数据/逻辑→其他两版降级为「风格参考」或「付费皮肤」）。
- 每版之间做「移植手册」：把统一的交互函数命名对齐（如统一 `goTab` 命名），降低日后合并成本。

### 决策建议

| 阶段 | 动作 |
|---|---|
| **现在（原型阶段）** | 维持三版独立文件，先让用户评审选定风格方向 |
| **开发启动时** | 采用方案一（Token 主题化）：用一个引擎 + 三主题；或方案二先以选定版为基线 |
| **上线后** | 换肤功能可作为差异化卖点（iOS 用户用奢华版皮肤等） |

---

## 五、如果要做「一键换 UI」demo（下一步可选）

我可以在 `prototypes/` 下新增一个 `theme-switcher.html`：单个页面内三台 iPhone 并排，顶栏一个「简约 / 时尚 / 奢华」切换器，点哪版哪个高亮并放大——把三版并到一个可切换的演示页里（引擎共用逻辑，视觉用三套 Token）。需要的话直接说。

---

## 六、风险与边界（诚实声明）

1. **三版不是「同一代码换皮」**：当前是三个独立 HTML，功能点一致但代码未共用；「直接更换」需要一次合并重构（约 1–2 天工作量）。
2. **结构性差异无法靠 CSS 变量全解决**：布局骨架差异（层次 B）必须组件化，Token 只能覆盖颜色/字体/圆角。
3. **若未来要真正「一套代码多主题」**：建议从 React 组件 + CSS 变量起步，而不是继续堆三个独立文件——否则维护成本随版本数线性增长。