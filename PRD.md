# 来财记账 · 产品需求文档（PRD）

> 版本 v1.0 ｜ 日期 2026-08-17 ｜ 状态：评审稿
> 依据：《自动记账App_GitHub竞品调研报告》（2026-08-17 版）＋产品决策访谈（2026-08-17）

---

## 0. 文档导读

本 PRD 是「来财记账」的唯一需求源头。目录下的三版高保真原型（`prototypes/`）、产品介绍 PPT（`deck/`）、产品信息图（`infographic/`）与总览页（`index.html`）均由本文档的功能点与数据模型派生，开发阶段应依此实现。

| 交付物 | 文件 | 用途 |
|---|---|---|
| 需求文档 | `PRD.md` | 唯一需求源头，用于评审、排期、验收 |
| 三版原型 | `prototypes/minimal.html`、`fashion.html`、`luxury.html` | 高保真交互原型（含 iPhone 设备框、动画），可直接作为 UI 开发参照 |
| 产品 PPT | `deck/index.html` | 路演/汇报，1920×1080 |
| 信息图 | `infographic/index.html` | 单页传播、架构说明 |
| 总览页 | `index.html` | 一次打开浏览全部交付物 |

---

## 1. 产品概述

### 1.1 一句话定位

> **来财记账 = Android 实时自动记账为尖刀、跨端账单导入为底座、云端 LLM 周期分析为增值的「确定优先」记账应用。**

### 1.2 背景与市场断层（调研结论）

- 目前**没有一个高 Star 开源项目同时完整实现**「Android+iOS、实时自动记账、第三方账单导入、大模型周期分析」。
- 高 Star 项目（Maybe 54.3k / Actual 28.2k / Firefly III 24.3k）强在账本、预算、报表与银行同步，**真正实时捕获手机交易的开源项目数量少、Star 普遍不高**（PennyWise 538 / Cashiro 166）。
- Mymo（私有，2.1.2）是最贴近中国用户原始需求的代码级参考，已解决通知解析、去重、置信度、待确认全链路，但**缺账单导入、AI 分析、跨设备同步**。
- 平台边界：Android 可读支付通知/短信；**iOS 不允许第三方 App 读取通知正文**。

### 1.3 核心原则（架构铁律）

| 铁律 | 内容 |
|---|---|
| T1 确定性优先 | 金额、借贷方向、去重、余额**必须**由确定性代码控制；LLM 只做分类建议、趋势解释、异常提示、自然语言分析 |
| T2 可追溯 | LLM 输出必须可下钻到产生结论的类别、商户、时间范围与原始交易 |
| T3 隐私最小化 | 通知正文不落盘；端侧数据不出站；云端分析需显式授权、可一键关闭 |
| T4 诚实边界 | 对外表述为「自动归集可获取的交易，并提供便捷导入与确认」，**不承诺**读取所有手机/支付平台/系统版本的每一笔交易 |

### 1.4 产品决策（已确认）

| 决策项 | 结论 |
|---|---|
| 平台策略 | **Android 先行，iOS 后补**：自动记账尖刀聚焦 Android；iOS 先做账本+导入+分享扩展 |
| 许可证 | **开源（MIT 或双许可）**：不直接复用 GPL 代码（Mymo/Cashew），只借鉴思路并重写；规避 GPL 传染 |
| AI 形态 | **云端 LLM（显式授权）**：最小化数据上传、用户可见授权、可关闭 |
| MVP 边界 | **直达阶段三**：包含自动记账、双端账本、账单导入、周期分析（LLM） |

---

## 2. 目标用户与场景

### 2.1 目标用户

| 画像 | 特征 | 核心诉求 |
|---|---|---|
| 年轻上班族（25–35，主力） | 微信/支付宝高频支付，嫌手动记账麻烦 | 自动入账、月底能看清钱去哪了 |
| 收支模糊的月光族 | 不知道自己每月花多少 | 周期总结、异常预警、节省建议 |
| 副业/自由职业者 | 多账户（工资+副业+理财）、多来源 | 多账户识别、多来源归集 |
| 隐私敏感用户 | 不愿账单上云 | 本地优先、端侧模型、可关闭云端 |

### 2.2 核心场景

- **S1 买完即记**：微信/支付宝/银行 App 支付后，通知被捕获 → 自动解析 → 高置信度直接入账，用户 0 操作。
- **S2 待确认兜底**：低置信度或分类不明确的记录进入「待确认」，用户 1 秒确认/修改/忽略，确认前不影响收支统计。
- **S3 历史账单导入**：从未用过的 App 用户，导入微信/支付宝导出的 CSV，自动映射、去重、入账。
- **S4 月底复盘**：月末打开「分析」，看到确定性月度指标 + LLM 生成的周期报告（可追溯、含覆盖度声明）。
- **S5 自然语言问答**：问「我这个月外卖花了多少？」「上周交通费环比变化」，LLM 基于已验证账本回答。

---

## 3. 需求功能点清单

> P0 = MVP 必做 ｜ P1 = MVP 后第一优先级 ｜ P2 = 远期
> 分解依据：调研报告 §9 建议实施优先级（阶段一→四）

### 3.1 A 自动记账（Android 核心 · P0）

| 编号 | 功能 | 说明 | 参考 |
|---|---|---|---|
| A1 | 来源预过滤 | 按包名/应用名/通知标题判断来源（微信/支付宝/银行），非目标通知在读取完整字段前直接丢弃 | Mymo 思路 |
| A2 | 通知正文提取 | 遍历 extras、MessagingStyle 嵌套字段、tickerText，限制最大文本长度 | 同上 |
| A3 | 确定性解析 | 金额模式、交易意图关键词、收支关键词、商户模式 → 结构化交易（金额整数分、方向、商户、时间） | 同上 |
| A4 | 置信度判定 | 金额候选评分 + 意图判定；置信度不足 → 进入待确认 | 同上 |
| A5 | 去重与合并 | SHA-256 externalId 去重；银行×钱包同一交易的跨渠道时间窗口合并 | 同上 |
| A6 | 自动入账/待确认 | 高置信度且分类明确 → 直接入账；其余 → 待确认区，确认前不影响收支与预算 | 同上 |
| A7 | 后台可靠性 | 重新绑定、退避重试、进程回收恢复、开机/更新恢复监听、Android 15 敏感正文处理 | 同上 |
| A8 | 账户自动匹配 | 按银行、卡号尾号自动匹配多账户（默认通用账户兜底） | Mymo 缺口① |

### 3.2 M 手动记账（双端 · P0）

| 编号 | 功能 | 说明 |
|---|---|---|
| M1 | 快速记一笔 | 金额键盘、收支切换、分类选择、账户、备注；支持连续记多笔 |
| M2 | 账户管理 | 多账户（银行卡/微信/支付宝/现金），支持余额初始化与调整 |
| M3 | 分类管理 | 预置分类体系（餐饮/交通/购物/居住/娱乐…）+ 自定义分类 + 图标与颜色 |

### 3.3 I 账本与查询（双端 · P0）

| 编号 | 功能 | 说明 |
|---|---|---|
| I1 | 流水列表 | 按时间倒序、分组日期；分页/虚拟滚动 |
| I2 | 筛选与搜索 | 按分类/账户/收支/时间范围筛选；关键词搜索商户与备注 |
| I3 | 交易详情/编辑 | 查看来源、证据（通知来源/导入源）、编辑、删除、撤销删除 |
| I4 | 首页总览 | 本月结余、支出、收入；最近流水；待确认角标；资产概览 |

### 3.4 X 账单导入（双端 · P0–P1）

| 编号 | 功能 | 说明 | 优先级 |
|---|---|---|---|
| X1 | 微信/支付宝 CSV | 官方导出 CSV 模板解析 + 去重导入 | P0（兜底主路径） |
| X2 | 通用 CSV/OFX/QIF | 通用导入映射器（列 → 标准字段），预览 + 确认 | P1 |
| X3 | PDF/OCR | 支付平台账单 PDF 与票据识别 | P1 |
| X4 | 共享扩展/文件导入 | iOS 重要替代入口 | P1（iOS 补） |
| X5 | 导入去重 | 与既有交易比对（金额+时间+来源窗口），避免重复入账 | P0 |

### 3.5 ANA 周期分析与 LLM（阶段三 · 云端显式授权）

| 编号 | 功能 | 说明 |
|---|---|---|
| ANA1 | 确定性月度指标 | 支出/收入/结余、分类占比、环比、Top 商户、预算偏差——全部由规则与统计代码计算 |
| ANA2 | 周期报告（LLM） | 基于指标生成自然语言总结、异常提示、节省建议；**包含分析周期、数据覆盖度、未确认账单数**声明 |
| ANA3 | 自然语言问答 | 问句 → 指标/查询 → LLM 组织回答；可下钻到原始交易 |
| ANA4 | 授权与开关 | 首次使用明确授权弹窗；设置中可一键关闭云端分析（保留确定性指标） |
| ANA5 | 数据最小化 | 仅上传问题所需指标/交易子集，不整库上云 |

### 3.6 S 设置与安全（双端 · P0）

| 编号 | 功能 | 说明 |
|---|---|---|
| S1 | 本地数据库 | 原生 SQLite（金额最小货币单位整数），替代 IndexedDB 弱持久性 |
| S2 | 加密备份/恢复 | PBKDF2-SHA256（高迭代）派生密钥 + AES-256-GCM 加密备份；导入前校验 |
| S3 | 隐私开关 | 云端分析授权、通知正文不落盘声明、数据导出 |
| S4 | 通知权限引导 | 首次启动引导开启通知使用权；状态页显示「监听已开启/已中断」 |

### 3.7 V 预算与目标（延后 · P2）

| 编号 | 功能 | 说明 |
|---|---|---|
| V1 | 预算 | 按月/分类预算、进度提示、超支预警 |
| V2 | 储蓄目标 | 目标进度、定期转入计划 |

### 3.8 非目标（明确不做，防范围蔓延）

- ❌ 不做 iOS 实时通知读取（系统限制，技术上不可行）
- ❌ 不做证券/股票行情类资产管理
- ❌ 不做社交分享、社区
- ❌ 不做银行 Open Banking 直连（阶段四视市场再评估）
- ❌ 不做多币种换汇（首版单一人民币本位）

---

## 4. 信息架构与页面地图

```
来财记账
├── 首页 I4（总览）          ← Tab1
│   ├── 本月卡片（结余+支收）
│   ├── 资产概览（多账户）
│   ├── 待确认入口（角标）
│   └── 最近流水（3–5条）
├── 流水 I1/I2/I3            ← Tab2
│   ├── 列表 + 筛选/搜索
│   ├── 交易详情（可编辑/删除）
│   └── 导入账单入口 X1–X5
├── 记一笔 M1                ← Tab3（中央大按钮）
│   ├── 金额键盘 + 收支切换
│   └── 分类/账户/备注
├── 分析 ANA                  ← Tab4
│   ├── 确定性指标面板 ANA1
│   ├── LLM 周期报告 ANA2（授权态）
│   └── 自然语言问答 ANA3
├── 我的 S                    ← Tab5
│   ├── 账户 M2 / 分类 M3 / 预算 V（占位）
│   ├── 通知监听状态 A7（Android）
│   ├── 隐私与授权 S3/S4
│   └── 备份恢复 S2
└── 全局：通知捕获动效页（App 启动演示/状态页）
```

---

## 5. 数据模型（核心实体）

```
Account 账户
  id, name, type(bank/wallet/cash), bank?, cardTail?, currency(CNY),
  balanceMinor(最小货币单位整数), sort, archived

Category 分类
  id, name, kind(INCOME/EXPENSE/TRANSFER), icon, color, parent?, sort

Transaction 交易
  id, accountId, categoryId, amountMinor(整数), direction(IN/OUT/TRANSFER),
  merchant, note, occurredAt(UTC毫秒), source(manual/import/notification),
  externalId?(SHA-256 去重键), notifySource?(wechat/alipay/bank:<name>),
  confidence(0-1), status(committed/pending/cancelled),
  createdAt, updatedAt

PendingReview 待确认
  id, txSnapshot(JSON), reason(lowConfidence/ambiguous/accountUnknown),
  reviewedAt?

Budget 预算（延后）
  id, categoryId?, month, limitMinor

AnalysisRun 分析记录
  id, periodStart, periodEnd, coverage(已确认/全部), pendingCount,
  metrics(JSON 确定性指标), llmReport?, createdAt
```

**约定**：金额一律以最小货币单位整数存储（分）；时间一律 UTC 毫秒；外部 ID 用 SHA-256(`${source}|${merchant}|${amount}|${timeWindow}`) 生成去重键。

---

## 6. 关键流程

### 6.1 自动记账链路（Android）

```
通知到达 → A1 来源预过滤(包名/标题) → A2 正文提取(限长)
  → A3 确定性解析(金额/方向/商户) → A5 去重(哈希+时间窗)
  → 置信度判定 A4
      ├─ 高置信+分类明确 → A6 直接入账（账本+统计即时更新）
      └─ 低置信/不明确 → 待确认（不参与收支与预算统计）
```

### 6.2 账单导入链路

```
选择来源(X1 微信/支付宝 CSV / X2 通用) → 解析映射器(列→标准字段)
  → 预览列表(标红冲突) → X5 导入去重 → 入账或并入待确认
```

### 6.3 周期分析链路（LLM 边界内）

```
账本数据 → ANA1 确定性指标计算(规则代码，无 LLM)
  → 组装「指标 + 覆盖度 + 未确认数」私密上下文
  → 用户显式授权 + 最小化上传 → ANA2 LLM 生成报告
  → 输出可追溯卡片（每个结论可下钻到原始交易）
  → 用户可关闭 → 回到纯确定性指标面板
```

---

## 7. 非功能需求

| 维度 | 要求 |
|---|---|
| 性能 | 10 万条交易列表可滚动不卡顿（虚拟滚动）；查询 <100ms；冷启动 <2s |
| 兼容 | Android 8.0+ 优先（通知监听稳定），Android 15 敏感通知桥接；iOS 15+（账本/导入） |
| 安全 | 通知正文仅在内存处理不落盘；备份 AES-256-GCM + PBKDF2 高迭代；导入校验 |
| 可用性 | 无不明确授权的后台联网；云端分析显式授权 + 一键关闭 |
| 可维护 | 解析规则样本库 + 自动化回归测试（对抗支付 App 文案变化） |
| 许可证 | 开源（MIT/双许可）；不引入 GPL 依赖 |

---

## 8. 里程碑与验收

> 优先级沿用调研报告 §9：阶段一 → 阶段二 → 阶段三 → 阶段四

| 阶段 | 内容 | 核心验收标准 |
|---|---|---|
| **阶段一：可信记账核心** | Android 通知捕获 A1–A8、手动记账 M、账本 I、通用 CSV 模板 X1、本地 SQLite S1 | 微信/支付宝/≥3 家银行通知可自动入账；误记率可观测；待确认流闭环 |
| **阶段二：跨端导入能力** | 双端 CSV/OFX/QIF、微信/支付宝官方账单 X1+X2、PDF/OCR X3、分享扩展 X4 | 主流账单格式一次导入去重正确；iOS 可独立使用（无自动捕获） |
| **阶段三：周期分析** | ANA1 确定性指标 → ANA2/3 云端 LLM（授权、可追溯、可关闭） | 报告结论全部可下钻；关闭云端后功能不降级 |
| **阶段四：连接与同步** | 银行/Open Banking 聚合、端到端加密跨设备同步（视市场） | 一个账户多端一致；同步冲突无丢账 |

**交付物验收**（本次原型交付）：
- [ ] PRD 覆盖上表全部 P0 功能点，且与三版原型一一对应
- [ ] 三版原型均为单文件可双击打开，含 iPhone 设备框、交互、动画
- [ ] 每版布局骨架互异（非换皮），可读性达标（正文 ≥14px，对比度 ≥4.5:1）
- [ ] 信息图与 PPT 由 PRD 同源派生，无新增未评审功能
- [ ] Playwright 冒烟：进详情 / 标注点 / Tab 切换，pageerror = 0

---

## 9. 风险与对策

| 风险 | 等级 | 对策 |
|---|---|---|
| iOS 无法实时自动记账，用户预期落差 | 高 | 对外一致表述「自动归集+便捷导入确认」；iOS 主打导入与账本 |
| 支付 App 通知文案变化导致解析失效 | 高 | 解析规则样本库 + 自动化回归 + 规则可热更新 |
| GPL 传染（若误用 Mymo 代码） | 中 | 只借鉴思路重写；依赖审计（MIT 优先）；法律复核 |
| 云端 LLM 隐私顾虑 | 中 | 最小化上传 + 显式授权 + 一键关闭 + 隐私声明页 |
| 解析误记账损害信任 | 中 | 置信度体系 + 待确认兜底 + 撤销删除 + 记录可追溯 |

---

*附：三版原型与本文档的映射关系见 `prototypes/README.md`（由原型构建完成后补充）。*